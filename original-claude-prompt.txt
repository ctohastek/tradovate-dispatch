
 You have superuser knowledge in rest apis and will build a python app that can run independently (say in a gunicorn setup), and send   
  API calls to the Tradovate futures platform (both live or demo URLs) to execute futures trades. This app should be highly              
  configurable by the administrator and will receive commands from an internal AI agent on another internal system, via POST data (or    
  whatever method you feel is a better fit) - the sending Agent will have analyzed a setup and provide post data for tradovate-dispatch  
  to execute on its behalf (thereby preventing the agent from knowing the access credentials on the remote tradovate account). Once
  trades are placed they will be monitored and managed by internal agents that will send further commands sequentially as needed until   
  the trade is complete - tradovate-dispatch only receives instructions then sends commands in accordance to the documentation found     
  here: https://api.tradovate.com/ <-- additionally in this local directory is a .json file specifying Tradovate's API spec and          
  protocols. In order to start up the app first a plan of research and development should be outlined. Ideally the sending agents
  should have unique identities verified by some authentication method for each call. The dispatch code should have guardrails that      
  prevent a rogue agent from switching target URLS (say to the live site using real money instead of the demo site which is a            
  simulation trading platform). With your vast knowledge of web application security should come more suggestions for architecture and   
  security implentation details. Additionally the dispatch application should be able to be proxied via nginx for example so that it     
  can be wrapped in TLS, and access to the dispatch app can be limited (by me) later to dedicated source IPs, as this should not be a    
  publicly visible application. Please let me know what tools you might need to complete work as it progresses. The application should   
  have a command reference with examples so that the agent calls can be configured by someone else administering the openclaw where the  
  agent(s) live. As well, there should be a deployment checklist describing how to migrate the finished dispatch app to a remote
  server. Of course any and all suggestions to improve upon this idea are quite welcome

